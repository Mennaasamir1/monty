#include "monty.h"
/**
 * to_q - implements the queue function
 * @stack: st
 * @line_number: line num
 */
void to_q(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;

	ptr->flag_it = 0;
}
