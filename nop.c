#include "monty.h"
/**
 * nothing_op - this function does nothing at all
 * @stack: stack
 * @line_number: line num
 */

void nothing_op(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
