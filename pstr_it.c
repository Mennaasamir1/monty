#include "monty.h"


void print_str(stack_t **stack, unsigned int line_number)
{
	stack_t *alt;

	(void) stack;

	alt = ptr->top;
	while (alt != NULL)
	{
		if (alt->n <= 0 || alt->n > 127)
		{
			break;
		}
	}
}
